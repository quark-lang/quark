(declare true bool)
(let true (var "true"))

(declare false bool)
(let false (var "false"))

(defm if [cond then else]
  (match cond
    [true then]
    [false else]))

(defn bool:show [x]
  (blue (match x
    [true "true"]
    [false "false"])))