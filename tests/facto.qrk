(import "std:prelude.qrk")
(data (Maybe a b) [
  Nothing
  (Just a b)
])

(defn facto [n]
  (match n
    [0 1]
    [_ (* n (facto (- n 1)))]))

(let m (map:insert "a" 1 (map:insert "b" 2 (map:insert "c" 3 map:empty))))
(defn main []
  (map:iter print m))