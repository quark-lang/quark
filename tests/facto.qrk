(import "test.qrk")
(data (Maybe a b) [
  Nothing
  (Just a b)
])
(defn facto [n]
  (match n
    [0 1]
    [_ (* n (facto (- n 1)))]))
(defn main []
  (match (Just 4 6)
    [(Just a b) (- a b)]
    [Nothing 5]))