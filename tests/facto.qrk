(import "types.qrk")
(import "path.qrk")
(import "types/list.qrk")

(let ack (fn [m n]
  (if (= m 0) (+ n 1)
    (if (= n 0) (ack (- m 1) 1)
      (ack (- m 1) (ack m (- n 1)))))))

(declare main (-> [str] int))
(defn main [args]
      (print_list (filter (fn [x] (= x "buh")) (Cons "buh" (Cons "test" Nil)))))