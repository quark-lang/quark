(data (Maybe a) [
  (Just a)
  Nothing
])

(data (List a) [
  (Cons a (List a))
  Nil
])

(data (IO a))

(declare (print a) (-> a (IO a)))
(declare (fromMaybe a) (-> a (Maybe a) a))
(declare (main a) (-> [str] (IO a)))

(let main (fn [args] (print 8)))